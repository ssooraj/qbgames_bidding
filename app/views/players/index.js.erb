$(".cd-container").append('<%= j render :partial => 'list' %>');
<% if @list.next_page %>
<% if @key %>
$('.pagination').replaceWith('<%= j will_paginate @users %>');
<% else %>
$('.pagination').replaceWith('<%= j will_paginate @users %>');
<% end %>
<% else %>
$('.pagination').remove();
<% end %>